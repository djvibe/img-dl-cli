[project]
name = "img-dl-cli"
version = "0.2.0"
description = "A resilient command-line tool for downloading high-resolution web image candidates."
readme = "README.md"
requires-python = ">=3.8"
license = { file = "LICENSE" }
authors = [
  { name = "djvibe", email = "your-email@example.com" },
]
keywords = ["google-images", "bing-images", "image-downloader", "cli", "selenium"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "Topic :: Internet :: WWW/HTTP :: Browsers",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "selenium",
    "webdriver-manager",
]

[project.scripts]
img-dl = "img_dl_cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["downloader", "img_dl_cli"]
